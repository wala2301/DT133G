The GitHub CLI (gh) is an open-source command-line tool that brings GitHub functionality directly into the terminal.
It supports operations such as creating and reviewing pull requests, managing issues, cloning repositories, and running GitHub Actions workflows.
Authentication is handled with gh auth login, which stores credentials securely and supports both browser-based OAuth and token-based flows.
GitHub CLI extensions allow community-developed commands to be installed and run alongside the built-in functionality.
