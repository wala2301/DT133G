The Factory Method design pattern defines an interface for creating an object but lets
subclasses or configuration decide which concrete class to instantiate. This decouples
client code from the specific classes it creates, making it easier to introduce new types
without modifying existing code. A common Python implementation uses a factory function
or a registry dictionary mapping string keys to class constructors. The Abstract Factory
pattern extends this to produce families of related objects through a common interface.