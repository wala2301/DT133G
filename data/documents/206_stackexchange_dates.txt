Dates in the API are formatted in Unix time, where all dates are rounded to the nearest second because the API doesn't
return fractions.
Date format YYYY-MM-DD HH:MM:SS is accepted to facilitate custom queries without a key.