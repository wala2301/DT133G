The GitHub advises developers to avoid polling the API un necessarily.
Instead, it recommends using webhooks where possible so integrations can react to events without repeatedly
querying the same resources.