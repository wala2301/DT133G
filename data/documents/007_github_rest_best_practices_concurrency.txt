Too many  concurrent requests can trigger secondary rate limits and therefore GitHub recommends avoiding this.
The placing requests them in the waiting list, and process them in a more controlled and sequential way is a safer pattern.